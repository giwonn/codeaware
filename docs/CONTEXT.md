# Codeaware - 프로젝트 맥락

## 왜 이 프로젝트를 만들게 됐는가

### 시장 공백 발견

코드 품질/레거시 코드 관련 MCP 생태계를 조사한 결과 핵심 공백 발견:
- **코드 품질 분석 도구는 많지만 전부 파편화** (SonarQube, CodeScene, Omen 등)
- **리팩토링 실행 도구는 언어별로 분리** (Python만, Java만, TS만)
- **MSA 분해는 MCP 생태계에 전무** (상용 도구만 존재: vFunction, IBM Mono2Micro)
- **"진단 → 계획 → 실행 → 검증"을 일관되게 하는 MCP는 없음**

그리고 결정적으로: **"AI가 이 코드를 얼마나 안전하게 다룰 수 있는가"라는 관점의 도구는 없음.**

기존 도구들은 cyclomatic complexity 같은 전통 메트릭만 측정하고, AI 시대에 진짜 중요한 질문에 답하지 못한다.

### 핵심 인사이트: "숨은 맥락" 탐지

기존 도구들이 전혀 못 하는 것:
- 코드 자체는 읽을 수 있는데, **"왜 이렇게 짰는지"가 코드 밖에 있는 경우**
- 매직 넘버에 설명 없음 (비즈니스 규칙이 코드에만 존재)
- 실행 순서에 의존하는 로직 (과거 장애 대응의 흔적)
- 주석 없는 예외 처리 분기 ("이유는 모르지만 빼면 안 됨")

이것이 AI가 "이해했다"고 판단하고 자신있게 수정했는데 장애가 나는 원인이다.

### 최종 결정

- **리팩토링은 하지 않는다** — Claude가 이미 잘 한다
- **진단과 방향 제시에만 집중** — 6단계 척도 + 개선 로드맵
- **아키텍처 전환(MSA, EDA 등)은 MCP prompt로 제공** — 코드로 자동화할 영역이 아님

## 기술 스택 결정 이유

- **TypeScript**: MCP 생태계 표준, 공식 SDK 지원, npx로 설치 없이 실행
- **Bun (Node.js 대신 선택한 이유)**:
  - Node.js는 파일 I/O를 libuv 스레드 풀에 위임 (기본 4개). 동시에 파일 100개 읽으면 4개씩 순차 처리 → 병목 발생
  - Bun은 libuv를 쓰지 않고 Linux의 io_uring을 사용하여 커널 레벨에서 비동기 I/O 처리. 스레드 풀 병목 없음
  - 프로젝트 파일 수백~수천 개를 스캔하는 이 도구의 특성상 파일 I/O 성능이 중요
  - 추가 장점: TypeScript를 빌드 없이 바로 실행 가능, 내장 테스트 러너 (`bun test`)
- **MCP SDK v1.x**: 안정 버전, stdio transport로 Claude Desktop/IDE 연동
- **Regex 기반 분석 (AST 아님)**: 외부 의존성 없이 모든 언어 지원. 진단 도구이므로 정밀도보다 범용성이 중요

## 경쟁 환경 요약

| 도구 | 코드 품질 평가 | AI 이해도 척도 | 숨은 맥락 탐지 | 개선 로드맵 |
|------|:---:|:---:|:---:|:---:|
| SonarQube MCP | O | X | X | X |
| CodeScene MCP | O | X | X | 부분 |
| Omen | O | X | X | 우선순위만 |
| OHM-MCP | O | X | X | O (Python만) |
| **codeaware** | O | **O** | **O** | **O** |
